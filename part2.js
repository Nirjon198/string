//now we will learn about string length
let a=`learn`;
console.log(a.length);
let b=`john
mathew
david`;
//here new line is also a character.so the output is 17,however there are 15 alphabet.
console.log(b.length);